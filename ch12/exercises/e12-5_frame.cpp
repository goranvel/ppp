#include "../../Simple_window.h";

int main (void) {
	Graph_lib::Point p (2500, 200);

	Simple_window win (p, 400, 400, "Canvas");

	win.set_label ("Rectangel dependedt on the size of scree3n";
	win.wait_for_button();
}
